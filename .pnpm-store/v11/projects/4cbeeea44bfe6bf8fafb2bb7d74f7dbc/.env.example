# Frontend environment template (copy to .env.local for local development).
# In development the Vite dev server proxies /api to the backend (see vite.config.ts),
# so this can usually be left unset. Set it only for non-proxied deployments.
# VITE_API_BASE_URL=/api/v1
