/** Shared AiSensy-reference surfaces for the dashboard cards (white, 8px radius, soft glow). */
export const DASH_CARD = "rounded-lg bg-surface shadow-card";

/** Dark teal compact action — AiSensy's "Setup" / "Buy More" button (31px tall, 6px radius). */
export const DASH_PRIMARY_BUTTON =
  "inline-flex h-[31px] items-center justify-center gap-1.5 rounded-md bg-[var(--color-nav-bg)] px-2.5 text-[13px] font-medium text-white transition-[background-color,box-shadow] duration-200 hover:bg-[#08393d] hover:shadow-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-focus";

/** Soft mint panel used for the next step and callouts. */
export const DASH_SOFT = "bg-[#ebf5f3] dark:bg-accent-soft";
